/* ============================================================
   StarBonds - Hoja de estilos compartida por todas las páginas
   Misma paleta que la app (app/app.css):
     #440C89  violeta intenso (primario)
     #1E0042  noche (fondo profundo)
     #FFFFFF  blanco
     #9958EA  violeta claro (acento)
     #5C317D  violeta medio (superficie)
     #000000  negro
   ============================================================ */

/* Variables de color: cámbialas aquí y se actualiza todo el sitio */
:root {
  /* Paleta base */
  --violeta:        #440c89;
  --noche:          #1e0042;
  --blanco:         #ffffff;
  --violeta-claro:  #9958ea;
  --violeta-medio:  #5c317d;
  --negro:          #000000;

  /* Roles (mismos nombres de siempre, ahora en violeta) */
  --color-fondo:       #14002e;  /* mezcla noche/negro */
  --color-superficie:  #25083f;  /* tarjetas */
  --color-primario:    #440c89;
  --color-acento:      #9958ea;
  --color-texto:       #f3ecff;
  --color-texto-suave: #b9a9d6;
  --color-borde:       rgba(153, 88, 234, 0.22);

  --radio: 14px;
  --sombra: 0 14px 40px rgba(0, 0, 0, 0.45);
}

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: "Segoe UI", system-ui, -apple-system, sans-serif;
  background:
    radial-gradient(1200px 600px at 80% -10%, rgba(68, 12, 137, 0.55), transparent 60%),
    radial-gradient(900px 500px at -10% 10%, rgba(153, 88, 234, 0.18), transparent 55%),
    var(--color-fondo);
  background-attachment: fixed;
  color: var(--color-texto);
  line-height: 1.6;
  min-height: 100vh;
}

/* ---------- Barra de navegación ---------- */
.navbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 18px 6%;
  background: rgba(30, 0, 66, 0.72);
  backdrop-filter: blur(10px);
  border-bottom: 1px solid var(--color-borde);
  position: sticky;
  top: 0;
  z-index: 10;
}

.logo {
  font-size: 1.5rem;
  font-weight: 800;
  color: var(--blanco);
  text-decoration: none;
  letter-spacing: 0.3px;
}

.logo span {
  color: var(--violeta-claro);
}

.nav-links {
  display: flex;
  gap: 28px;
  list-style: none;
}

.nav-links a {
  color: var(--color-texto-suave);
  text-decoration: none;
  font-weight: 600;
  transition: color 0.2s;
}

.nav-links a:hover,
.nav-links a.activo {
  color: var(--blanco);
}

/* ---------- Botones ---------- */
.btn {
  display: inline-block;
  padding: 12px 26px;
  border-radius: 999px;
  font-weight: 700;
  text-decoration: none;
  cursor: pointer;
  border: none;
  font-family: inherit;
  transition: transform 0.15s, opacity 0.2s, box-shadow 0.2s;
}

.btn:hover {
  transform: translateY(-2px);
}

.btn-primario {
  background: linear-gradient(135deg, var(--violeta), var(--violeta-claro));
  color: #fff;
  box-shadow: 0 8px 22px rgba(68, 12, 137, 0.55);
}

.btn-acento {
  background: var(--violeta-claro);
  color: #1a0033;
}

.btn-borde {
  background: transparent;
  color: var(--color-texto);
  border: 1.5px solid var(--violeta-claro);
}

.btn-borde:hover {
  background: rgba(153, 88, 234, 0.14);
}

/* ---------- Secciones generales ---------- */
.contenedor {
  max-width: 1100px;
  margin: 0 auto;
  padding: 0 6%;
}

.hero {
  text-align: center;
  padding: 90px 6%;
}

.hero h1 {
  font-size: 3rem;
  margin-bottom: 18px;
}

.hero h1 span {
  color: var(--violeta-claro);
}

.hero p {
  max-width: 640px;
  margin: 0 auto 32px;
  color: var(--color-texto-suave);
  font-size: 1.15rem;
}

.seccion {
  padding: 60px 0;
}

.seccion h2 {
  font-size: 2rem;
  margin-bottom: 24px;
  text-align: center;
}

/* Sección de dos columnas: video a la izquierda, descripción a la derecha */
.video-info {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 30px;
  align-items: center;
}

/* Marco que mantiene el video en proporción 16:9 (panorámico) */
.video-marco {
  position: relative;
  width: 100%;
  padding-top: 56.25%; /* 16:9 */
  background: linear-gradient(135deg, var(--violeta-medio), var(--noche));
  border: 1px solid var(--color-borde);
  border-radius: var(--radio);
  overflow: hidden;
  box-shadow: var(--sombra);
}

.video-marco iframe,
.video-marco video {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  border: none;
}

/* En pantallas pequeñas se apilan (video arriba, texto abajo) */
@media (max-width: 700px) {
  .video-info {
    grid-template-columns: 1fr;
  }
}

/* Tarjetas reutilizables (misión/visión, comunidad, etc.) */
.grid {
  display: grid;
  gap: 24px;
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
}

.tarjeta {
  background: var(--color-superficie);
  border: 1px solid var(--color-borde);
  border-radius: var(--radio);
  padding: 28px;
  box-shadow: var(--sombra);
  transition: transform 0.18s, border-color 0.2s;
}

.tarjeta:hover {
  transform: translateY(-4px);
  border-color: var(--violeta-claro);
}

.tarjeta h3 {
  margin-bottom: 12px;
  color: var(--violeta-claro);
}

/* Marcador de contenido editable: te indica dónde poner tu info */
.editable {
  border: 2px dashed var(--violeta-claro);
  border-radius: var(--radio);
  padding: 18px;
  color: var(--color-texto-suave);
  font-style: italic;
  background: rgba(153, 88, 234, 0.08);
}

/* ---------- Formulario de registro ---------- */
.form-registro {
  max-width: 460px;
  margin: 0 auto;
  background: var(--color-superficie);
  border: 1px solid var(--color-borde);
  padding: 36px;
  border-radius: var(--radio);
  box-shadow: var(--sombra);
}

.campo {
  margin-bottom: 18px;
}

.campo label {
  display: block;
  margin-bottom: 6px;
  font-weight: 600;
}

.campo input,
.campo select {
  width: 100%;
  padding: 12px 14px;
  border-radius: 10px;
  border: 1px solid var(--color-borde);
  background: var(--color-fondo);
  color: var(--color-texto);
  font-size: 1rem;
  font-family: inherit;
}

.campo input:focus,
.campo select:focus {
  outline: none;
  border-color: var(--violeta-claro);
}

/* ---------- Tabla de precios ---------- */
.precios {
  display: grid;
  gap: 24px;
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  align-items: stretch;
}

.plan {
  background: var(--color-superficie);
  border-radius: var(--radio);
  padding: 32px;
  text-align: center;
  border: 1px solid var(--color-borde);
  display: flex;
  flex-direction: column;
}

.plan.destacado {
  border-color: var(--violeta-claro);
  box-shadow: 0 0 0 1px rgba(153, 88, 234, 0.35), var(--sombra);
}

.plan .precio {
  font-size: 2.4rem;
  font-weight: 800;
  margin: 12px 0;
  color: var(--violeta-claro);
}

.plan ul {
  list-style: none;
  margin: 20px 0;
  text-align: left;
  flex-grow: 1;
}

.plan ul li {
  padding: 8px 0;
  border-bottom: 1px solid var(--color-borde);
  color: var(--color-texto-suave);
}

/* ---------- Pie de página ---------- */
.footer {
  background: rgba(30, 0, 66, 0.55);
  border-top: 1px solid var(--color-borde);
  text-align: center;
  padding: 30px 6%;
  color: var(--color-texto-suave);
  margin-top: 40px;
}

/* ---------- Responsivo ---------- */
@media (max-width: 700px) {
  .nav-links {
    gap: 16px;
    font-size: 0.9rem;
  }
  .hero h1 {
    font-size: 2.2rem;
  }
}
